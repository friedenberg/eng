#! /bin/bash -xe

# dir="$(git rev-parse --show-toplevel)"                                                                                                                                         
# pushd "$dir" || exit 1
# git secret reveal -f
